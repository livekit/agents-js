---
'@livekit/agents-plugin-google': patch
---

Set `propertyOrdering` on every object in a Live API tool's parameter schema, so Gemini generates the arguments in the order the tool declares them instead of alphabetically.
