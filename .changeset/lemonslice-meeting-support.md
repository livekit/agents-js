---
'@livekit/agents-plugin-lemonslice': patch
---

Add external meeting support to the LemonSlice avatar plugin: `joinMeeting()` and `leaveMeeting()` send the avatar into Zoom, Google Meet, Microsoft Teams, or Webex meetings, stream mixed meeting audio into the agent session's STT, and optionally relay meeting chat messages as user input.
