{
  "name": "@types/livekit-agents-plugin-openai",
  "version": "0.1.0",
  "description": "Type definitions for @livekit/agents-plugin-openai",
  "types": "dist/index.d.ts",
  "scripts": {
    "build": "tsc",
    "clean": "rm -rf dist",
    "clean:build": "pnpm clean && pnpm build"
  },
  "author": "LiveKit",
  "devDependencies": {
    "typescript": "^5.4.5"
  },
  "peerDependencies": {
    "@livekit/agents-plugin-openai": "workspace:*"
  }
}
